[package]
name = "compose"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1.1.2+spec-1.1.0"
anyhow = "1"
types = { path = "../types" }
paths = { path = "../paths" }

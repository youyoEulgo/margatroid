[package]
name = "skill_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
core_plugin = { path = "../../mecs/core_plugin" }
serde = { workspace = true }
toml = "0.8"

[dev-dependencies]
tempfile = "3"

[package]
name = "server_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
app_runtime_plugin = { path = "../app_runtime_plugin" }
axum = { workspace = true }
core_plugin = { path = "../core_plugin" }
futures-util = { workspace = true }
http_server_plugin = { path = "../http_server_plugin" }
log_plugin = { path = "../log_plugin" }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
tower = "0.5"

[package]
name = "margatroid_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
margatroid_types = { path = "../types" }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

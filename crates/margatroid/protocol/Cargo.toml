[package]
name = "margatroid_protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
agent_plugin = { path = "../agent_plugin" }
core_plugin = { path = "../../mecs/core_plugin" }
log_plugin = { path = "../../mecs/log_plugin" }
margatroid_types = { path = "../types" }
memory_plugin = { path = "../memory_plugin" }
server_plugin = { path = "../../mecs/server_plugin" }
serde = { workspace = true }
workspace_plugin = { path = "../workspace_plugin" }

[dev-dependencies]
app_runtime_plugin = { path = "../../mecs/app_runtime_plugin" }
serde_json = { workspace = true }
tool_plugin = { path = "../tool_plugin" }

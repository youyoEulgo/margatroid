[package]
name = "margatroid_defaults"
version = "0.1.0"
edition = "2024"

[dependencies]
app_runtime_plugin = { path = "../../mecs/app_runtime_plugin" }
async_runtime_plugin = { path = "../../mecs/async_runtime_plugin" }
config_plugin = { path = "../config_plugin" }
core_plugin = { path = "../../mecs/core_plugin" }
event_bus_plugin = { path = "../event_bus_plugin" }
external_event_plugin = { path = "../../mecs/external_event_plugin" }
http_server_plugin = { path = "../../mecs/http_server_plugin" }
llm_plugin = { path = "../llm_plugin" }
log_plugin = { path = "../../mecs/log_plugin" }
sandbox_plugin = { path = "../sandbox_plugin" }
server_plugin = { path = "../server_plugin" }
skill_plugin = { path = "../skill_plugin" }

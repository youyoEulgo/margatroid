{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute path or project-root-relative path of the UTF-8 text file to edit."
    },
    "old_string": {
      "type": "string",
      "minLength": 1,
      "description": "Exact literal text to replace."
    },
    "new_string": {
      "type": "string",
      "description": "Replacement text. Use an empty string to delete the matched text."
    },
    "replace_all": {
      "type": "boolean",
      "default": false,
      "description": "Replace every occurrence. Defaults to false, which requires exactly one match."
    }
  },
  "required": ["path", "old_string", "new_string"],
  "additionalProperties": false
}

schema_version = 1
name = "grep"
description = "Search file contents with a ripgrep regular expression. Returns matching paths, line numbers, and line previews. Use read-file on a match when surrounding context is needed."

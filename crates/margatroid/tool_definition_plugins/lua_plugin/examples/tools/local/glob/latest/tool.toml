schema_version = 1
name = "glob"
description = "Find files whose paths match a glob pattern. Returns files only, including hidden and ignored files while excluding version-control metadata directories."

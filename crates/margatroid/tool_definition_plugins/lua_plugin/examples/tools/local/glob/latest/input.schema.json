{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "pattern": {
      "type": "string",
      "minLength": 1,
      "description": "Glob pattern to match file paths, for example **/*.rs. A pattern without a slash matches basenames at any depth."
    },
    "path": {
      "type": "string",
      "minLength": 1,
      "default": ".",
      "description": "Directory to search. Relative paths resolve from the project root."
    }
  },
  "required": ["pattern"],
  "additionalProperties": false
}

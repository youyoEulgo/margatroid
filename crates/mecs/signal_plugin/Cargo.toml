[package]
name = "signal_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
app_runtime_plugin = { path = "../app_runtime_plugin" }
core_plugin = { path = "../core_plugin" }
signal-hook = "0.3"
tracing = { workspace = true }

[package]
name = "config_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
core_plugin = { path = "../core_plugin" }
serde = { workspace = true }
toml = "0.8"
types = { path = "../../types" }

[dev-dependencies]
tempfile = "3"

[package]
name = "sandbox_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
async_runtime_plugin = { path = "../async_runtime_plugin" }
core_plugin = { path = "../core_plugin" }
sandbox = { path = "../../sandbox" }
tokio = { workspace = true }
